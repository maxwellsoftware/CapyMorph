# CapyMorph dependencies
# pympq — StormLib binding, reads/writes MPQ archives (Stages 1-5)
pympq>=0.2.2
# PySide6 — GUI (Stage 6)
PySide6>=6.6
# Pillow — decodes BLP2/DXT inventory icons for in-app preview
Pillow>=10.0
